import 'package:flutter/material.dart';

class AppColorScheme {
  static const deepTeal = Color(0xFF052F35); // primary background
  static const primaryColor = Color(0xFF04697D); // accent or primary button
  static const primaryDark = Color(0xFF034B57); // accent or primary button
  static const primaryLight = Color.fromARGB(255, 216, 229, 231);
  static const tealDark = Color(0xFF03414A);
  static const yellowAccent = Color(0xFFFFC107); // icons, highlights

  static const goldLight = Color(0xFFFFD700);
  static const gold = Color(0xFFFFC107);
  static const goldDeep = Color(0xFFFFA000);

  static const orangeAccent = Color(0xFFFF9149);

  // Background colors
  static const backgroundGrey = Color(0xFFF5F5F5);

  static const serviceColors = [
    Color(0xFF3B82F6), // Blue
    Color(0xFFF59E0B), // Orange
    Color(0xFFEF4444), // Red
    Color(0xFF8B5CF6), // Purple
    Color(0xFF10B981), // Green
    Color(0xFF6366F1), // Indigo
    Color(0xFFEC4899), // Pink
  ];
}
